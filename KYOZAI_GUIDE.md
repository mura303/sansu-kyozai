# 教材作成ガイド(AIエージェント向け)

新しい単元のインタラクティブ教材HTMLを作成するための仕様書。
このリポジトリの既存教材(年齢算・過不足算・つるかめ算・弁償算)が
実装の見本であり、**新教材は既存教材をコピーして中身を差し替える方式で作ること**
(どれを土台にするかは第4章の手順4)。
ゼロから書くとデザインや関数の細部がずれるため禁止。

対象読者は「算数が苦手な中学受験生」。この前提がすべての設計判断の基準になる。

---

## 1. 教育設計の原則(最重要)

### 1-1. 二段構えの構成: 表 → 線分図

すべての教材は次の2部構成にする。

- **表パート(前半)**: 具体的な数を書き出す・数え上げる操作を通して、その単元の
  核心法則を子ども自身に「発見」させる。表は遅いが確実な最後の手段であり、
  この安心感自体が苦手な子の支えになる。
- **線分図パート(後半)**: 発見した法則を図の「型」として抽象化し、
  同じ手順でどんな数字でも解けることを体験させる。

線分図の型を先に教えてはならない。表で法則を体感する前に型を与えると丸暗記になり応用が利かない。

### 1-2. 1ステップ1要素

各ステップで新しく登場する概念・操作は必ず1つだけ。迷ったら分割する。
ステップ数の目安は14〜18(始・終を除く)。「多すぎるかも」と感じるくらいでちょうどよい。

例(過不足算): 「差=65本」と「1人あたり5本」と「65÷5=13人」は3つの別ステップに分けた。

### 1-3. 核心の発見をクイズにする

その単元の核心法則は、説明するのではなく、子どもが答えるクイズとして山場に置く。

- 年齢算の核心 = 「年齢差は何年たっても変わらない」
  → 山場クイズ: 「(表にない)100年後の差はいくつ?」
- 過不足算の核心 = 「あまり+不足=全体の差、それは1人あたりの差×人数」
  → 山場クイズ: 「なぜ1人増えるとくいちがいが5縮む?」「2本のテープの差は何本?」

新単元を作るときは、まず「この単元の核心法則は何か」「それを発見させるクイズは何か」を
決めてから全体を設計する。

### 1-4. 「仮に決めて計算 → くいちがい発見 → 表で追いつめる」

未知数がある問題では、いきなり式を立てず「もし10人だったら?」と仮の数で
2通りの計算をさせ、答えが食い違うことに驚かせてから、表で正解に近づけていく。
この「おかしい!」という体験が探究の動機になる。

**例外: 仮定法が苦手な学習者向けの教材を作るときはこの原則を外す。**
「もし〜だったら」という反実仮想は、それ自体が一段むずかしい推論であり、
ここでつまずく子には「おかしい!」の驚きが起きず、混乱だけが残る。
その場合は第6章の型(図を長方形に整える操作として説明する)を使うこと。

### 1-5. 各ステップは単独で完結させる(再掲の原則)

**前のステップで作った表や図を参照するステップには、その表・図を画面内に必ず再掲する。**
ステッパー形式では前の画面が見えないため、「さっきの表を見て」は成立しない。
これはユーザーテストで実際に指摘された欠陥。再掲を忘れやすいので、
完成後に全ステップを1つずつ開いて「この画面だけで解けるか」を確認すること。

### 1-6. 誤答時は答えを教えず、ヒントだけ

不正解のフィードバックは「考え直すきっかけ」のみ。答えや解き方の全体は書かない。

- 良い例: 「②が28歳なら、①はその半分だね。」
- 悪い例: 「答えは14です。28÷2=14だからです。」

複数空欄の練習問題では、間違えた欄に対応するヒントだけを出す(実装は `ckAll()` 参照)。

### 1-7. 選択肢の誤答は「典型的なつまずき」にする

多肢選択の誤答はでたらめではなく、その単元で実際に起きる誤解を置く。

- 年齢算: 「38÷3をしたくなる」→ 誤答に反映
- 過不足算: 「あまり/不足の意味の取り違え」「31や34(全体の数)と5(1人あたり)の混同」

### 1-8. 締めくくりの3点セット

- 線分図パートの答えが出たら、表パートで得た答えと一致することを明示する
  (「表で見つけた答えと同じ!」)。2つの方法がつながる瞬間が理解の証。
- 検算を入れる(過不足算: 10×13−34=96 でも同じ96本になる)。
- 最後に数字を変えた同型の練習問題(空欄穴埋め形式)で型の定着を確認する。
- ゴール画面では型を一文で言語化し、次のパターンへの予告を一言添える
  (例: 「あまり同士の問題では差が引き算になる」)。

---

## 2. UIパターン集(既存実装の関数をそのまま使う)

| パターン | 用途 | 実装 |
|---|---|---|
| 多肢選択クイズ | 概念確認・核心の発見 | `mc(this, true/false, step, msg)` |
| 数値入力+答え合わせ | 計算をさせる | `ck(step, 答え, 正解時メッセージ)` + `hints` 辞書 |
| 表を育てる | +1ボタンで1行ずつ追加し変化を観察させる | `addRow()` を単元に合わせて改造 |
| 表の行タップ | 表から答えを探させる | `t3` 生成ループを改造 |
| テープ図の段階表示 | 線分図を1要素ずつ登場させる | `.trow` / `.tape` / `.brace` のHTML |
| 正解時に図が伸びる | 答えた瞬間に図が完成する演出 | `momOk()` 方式(正解処理内でHTML注入) |
| 複数空欄の練習問題 | 型の総仕上げ | `ckAll()`(欄ごとの個別ヒント付き) |

ナビゲーション(全教材共通・削除禁止):

- プログレスバー + 「n / N」表示
- 「← 戻る」ボタン
- ステップジャンプナビ(`始, 1, 2, …, 終`)— 復習時に途中から入るための必須機能
- 正解時のみ出現する「次へ →」ボタン(`nextBtn()`)

---

## 3. 技術規約

### 3-1. ファイル規約

- 1教材 = 1つの自己完結HTMLファイル。外部依存は Google Fonts のみ。
- ファイル名: 半角英数とハイフンのみ。`<単元ローマ字>-kyozai.html`(例: `tsurukamezan-kyozai.html`)。
- `index.html` という名前は使用禁止(デプロイ時に自動生成で上書きされる)。
- リポジトリ直下に置く。置いて push するだけで目次に自動掲載される。

### 3-2. メタ情報規約(目次自動生成が読み取る)

```html
<meta charset="utf-8">   <!-- 必須。欠くと文字化けする(実際に起きた事故) -->
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="description" content="目次カードに出す説明文。問題の概要+ステップ数。">
<meta name="course-order" content="3">   <!-- 目次での並び順 -->
<title>つるかめ算マスター — 表と面積図を15ステップで</title>
```

`<title>` は必ず「教材名 — 説明」の形式(区切りは全角ダーシ「—」)。
「—」の前が目次カードの見出しになる。

### 3-3. デザイントークン(変更禁止)

既存教材の `<style>` ブロックをそのままコピーする。要点:

- フォント: Zen Maru Gothic(丸ゴシック=子ども向けの親しみやすさ)
- 背景: 方眼紙(算数ノート)モチーフのグリッド
- 色: `--kid`(緑系)= 子ども・第1の量、`--mom`(オレンジ系)= 大人・第2の量、
  `--marker`(黄)= 問題文のマーカー、`--ok`/`--ng` = 正誤フィードバック
- カード: 太い縁取り+オフセット影(手作り教材感)

### 3-4. パネル構造

```
p0        = はじめに(問題文を .problem で提示)
p1..pN    = ステップ1..N(各パネル冒頭に .step-tag)
p(N+1)    = ゴール(型の言語化+次の予告+最初からやり直すボタン)
```

パネル数を変えたら、`jumpnav` の `labels` 配列と `.prog-label` の初期表示
(`1 / パネル総数`)を必ず一致させる。

---

## 4. 制作フロー(この順で作業する)

1. **問題を完全に解く。** 表による解法と線分図(または面積図)による解法の両方で解き、
   答えと検算を確定させる。
2. **核心法則と山場クイズを決める**(原則1-3)。
3. **ステップ表を書く。** 各行 = ステップ番号 / 学ぶこと1つ / UIパターン / 正答 / 誤答ヒント。
   1ステップ1要素になっているか、この段階で点検する。
4. **既存教材をコピーしてHTML化する。** 構造が近いものを土台にする:
   線分図単元 → `nenreizan-kyozai.html` / `kabusokuzan-kyozai.html`、
   面積図単元 → `tsurukamezan-kyozai.html`、
   マイナスの量がある単元・仮定法を使わない単元 → `benshozan-kyozai.html`(第6章)。
5. **メタ情報規約(3-2)を満たす。**
6. **セルフテストする。** 可能ならブラウザ相当の環境で全ステップを実際に操作し、
   少なくとも次のチェックリストを通す。

### 公開前チェックリスト

- [ ] `<meta charset="utf-8">` がある
- [ ] title が「教材名 — 説明」形式
- [ ] 全ステップが「その画面の情報だけ」で解ける(再掲の原則)
- [ ] 正答・計算がすべて正しい(表の数値、図の本数、練習問題含む)
- [ ] 誤答フィードバックが答えをばらしていない
- [ ] jumpnav のラベル数 = パネル数
- [ ] 表パートの答えと線分図パートの答えの一致を明示している
- [ ] 練習問題(数字違いの同型)がある
- [ ] (面積図単元のみ)図の読み方だけを学ぶ導入ステップがある(5-2)
- [ ] (面積図単元のみ)はみ出し部分と表パートの核心発見の対応を明示している(5-3)
- [ ] (仮定法を使わない教材のみ)「もし全部〜なら」という反実仮想がどこにも出てこない(6-2)
- [ ] (仮定法を使わない教材のみ)練習問題の直前に型カードのステップがある(6-4)
- [ ] 全選択肢・全入力欄を試して JS エラーが出ない

---

## 5. 面積図を使う単元(つるかめ算など)

線分図ではなく面積図が標準解法の単元(つるかめ算、平均算、食塩水の一部など)では、
以下のとおり調整する。**表パート(第1部)の設計原則は一切変わらない。**

### 5-1. 表パートは線分図単元と同型

つるかめ算の例(ツルとカメ合わせて10匹、足32本):
「仮に10匹全部ツルなら足は20本」→「実際は32本、12本のくいちがい」→
「1匹ずつカメに置き換える表を育てる」→「足が2ずつ増える」→
「なぜ2ずつ?(カメは足が 4−2=2本多い)」→「32本の行をタップ」。
核心発見 = 「置き換え1匹あたり2本」。原則1-4(仮に決めて計算)がそのまま使える。

### 5-2. 面積図は「新概念」— 導入専用ステップを必ず置く

線分図は「長さ=量」で直感的だが、面積図は
「たて(1つあたりの量)× よこ(個数)= 面積(合計)」という二次元の対応を
理解していないと読めない。図パートの冒頭に、**問題から離れて図の読み方だけを学ぶ
ステップを1〜2個**置くこと(例: 「2本 × 10羽 = 20本 を長方形で表すと、
たては何を表している?」)。この導入を飛ばすと後半が全部崩れる。

### 5-3. 図の構成要素も1ステップ1つずつ登場させる

つるかめ算での標準分解:
ツルの長方形(たて2)→ カメの長方形(たて4)→ 横の合計=10匹 →
全体の面積=足の合計32 → 「全部ツル」の帯(表パートの仮定の再利用)→
はみ出し部分の面積 = 32−20 = 12 → はみ出しのたて = 4−2 = 2 →
はみ出しのよこ = カメの数 = 12÷2 = 6匹。

**「はみ出しのたて2本」= 表で発見した「置き換え1匹で2本」であることを明示する。**
これが表と図の接続点(線分図単元の「表の答えと同じ!」に相当)。

### 5-4. 面積図ならではのUIパターン: スライダー

カメの数を `input[type=range]`(0〜10)で動かすと、面積図の分割位置と
足の合計表示がリアルタイムに変わり、「合計が32本になる場所」を図の上で探せる。
表の「+1匹」ボタンと同じ試行錯誤を図の上で再体験でき、表→図の橋渡しになる。
線分図単元にはないパターンなので、面積図単元では積極的に使う。

### 5-5. 実装メモ

- 長方形は div の入れ子か SVG。幅・高さは量に比例させる(例: 1匹=32px、足1本=28px)。
- 色は既存トークンを流用: 第1の量(ツル)= `--kid` 系、第2の量(カメ)= `--mom` 系、
  はみ出し部分は濃色(`.tp-over` 相当)で強調。
- たて・よこ・面積のラベル(「2本」「10匹」「20本」)は図の中に直接書く。
- スライダー実装: `oninput` で分割位置の div 幅と合計表示を更新するだけ。ライブラリ不要。

### 5-6. 図の選び方の原理(線分図か面積図か)

新単元でどちらの図を使うか迷ったら、問題の核心を式に書いて未知数の形を見る。

- **未知数が共通因子としてくくれる** → 線分図。
  例: 過不足算は 5×n と 10×n(n が共通)。「単価×個数」を1本のテープの長さとして
  丸ごと扱えるため、量が1種類(合計)だけになり1次元で足りる。
  「1つあたりの差」も、テープの**長さの差**としてそのまま図に見える。
- **異なる単価が異なる未知数に掛かり、合計に制約がある** → 面積図。
  例: つるかめ算は 2×ツル + 4×カメ = 32、ツル+カメ = 10。
  「1つあたりの量」と「個数」の2種類が同時に情報を持つため、
  たて(1つあたり)× よこ(個数)= 面積(合計)の2次元が必要。
  「1つあたりの差」は**たての差(はみ出しのたて)**として図に見える。

一言でいえば、**線分図はたし算・ひき算の関係を見る道具、
面積図は「掛け算の2つの因子が両方動く」関係を見る道具**。

注意: つるかめ算も「全部ツルなら20本、実際は32本、差12本」までは線分図で描けてしまう。
しかしその図では「1匹置きかえると2本増える」理由(4−2)が見えず、暗記になる。
**核心法則の理由が図の上に見える方**を選ぶこと。平均算・食塩水では
単価にあたる軸(点数・濃度)そのものが主役になるため、面積図が必須になる。

### 5-7. 単元ごとの図の使い分け(参考)

- 年齢算・差集め算・過不足算・分配算 → 線分図(テープ図)
- つるかめ算・平均算・食塩水(混合)→ 面積図(本章の指針を適用)
- 弁償算(勝てば上がる/負ければ下がる、割ると弁償する等)→ **地下つき面積図**。
  マイナスの量を地面より下に描く。詳細は第6章
- 植木算・日暦算 → 絵図(木と間隔など)。図の読み方導入(5-2)の考え方を同様に適用
- 仕事算 → 線分図または表のみでも可

ただし第1部が「表(書き出し)による発見」であることは全単元共通。

---

## 6. 仮定法を使わない教材(機械的な面積計算で解く)

原則1-4 の「もし全部〜だったら」は、反実仮想という一段むずかしい推論を要求する。
ここでつまずく学習者には、**仮定をいっさい立てず、図の面積を順に計算するだけ**の
型を用意する。実装の見本は `benshozan-kyozai.html`(弁償算)。

この章は「仮定法が苦手」とわかっている学習者向けの教材を作るときだけ適用する。
通常はこれまでどおり1-4を使うこと。

### 6-1. 弁償算の「地下つき面積図」

弁償算(勝てば3段上がる/負ければ1段下がる、無事なら30円もらえる/割れば70円弁償する)は、
**片方がマイナスの量**である点がつるかめ算とちがう。

- 地面の線(はじめの位置・ゼロ)を引き、**上がる量は地上、下がる量は地下**に描く。
- よこは両方とも「回数・個数」。よこの合計が問題文の総数になる(**和は並べる**)。
- 上の面積 − 下の面積 = 実際の値(**差は重ねる**)。

これで手がかりが2つそろう。「よこの合計」と「面積の差」だけで解ける。

### 6-2. 核心の一手: ☆をたしても差は変わらない

でこぼこの2つの図形のままでは計算できない。そこで
**○にも×にも同じ「☆」をたしても、差は変わらない**という性質を使う。

弁償算では ☆ =「勝ちの列の地下1段ぶん」を選ぶ。すると:

- **○+☆** = 勝ちの列がひとつながりの長方形。たて = 上のたて + 下のたて = 3+1 = 4段
- **×+☆** = 地下1段の帯が全幅につながる。よこ = よこの合計 = 20回 → 面積 = 1×20 = 20段
- 差は24段のままなので ○+☆ = 20+24 = 44段 → 勝ち = 44 ÷ 4 = 11回

仮定はどこにも出てこない。**図を長方形に整える操作**と、かけ算・たし算・わり算だけ。
「もし全部〜なら」という語をひとつも書かないこと(チェックリスト参照)。

なお、つるかめ算のように和(差ではなく合計)が与えられる単元で同じ方針を取るなら、
「低いほうのたてで全幅に帯を敷く → 残りの面積 ÷ たての差」と言いかえる。
計算は仮定法と同一だが、**反実仮想ではなく図形の分解として説明する**ので、
つまずきの原因を避けられる。

### 6-3. 表パート(第1部)の作り替え

表パートは残す(1-1 は全単元共通)。ただし仮定から始めない。

1. **関係式を具体例1つで作る。** 「勝ち5回・負け15回の子」を出し、
   上がった段(3×5=15)・下がった段(1×15=15)・いる場所(15−15=0)を順に計算させる。
   ここで **「上がった量 − 下がった量 = 実際の値」** が手に入る。
   これは仮定ではなく、ただの例。子はこの子の話として計算するだけでよい。
2. **その式のまま表を育てる。** 上の例を1行目にして、勝ちを1回ずつ増やし目標値を探す。
3. **核心発見クイズは変えない。** 「なぜ1回で4段ずつ動く?」→「上がる分が3ふえ、
   同時に下がる分が1へるから 3+1=4」。この 4 を後半の図で再発見させる(1-3・5-3と同じ)。

図の読み方導入(5-2)の題材も、「全部勝ちなら3×20=60段」ではなく
**上の具体例(3段×5回=15段)**を使う。ここに仮定を持ち込まないため。

### 6-4. 型カードのステップを必ず置く

仮定法を避ける学習者は「考え方」より**手順の確実さ**を支えにする。
練習問題の直前に、表1枚だけのステップ(クイズなし・`next` ボタン直結)を置き、
毎回同じ計算手順を明示する。弁償算の例:

| やること | じゃんけんでは |
|---|---|
| ① 地下の帯 = 下のたて × よこの合計 | 1 × 20 = 20 |
| ② 上の長方形 = ① + 面積の差 | 20 + 24 = 44 |
| ③ 答え = ② ÷ (上のたて + 下のたて) | 44 ÷ (3+1) = 11 |

練習問題はこのカードと**同じ番号・同じ順序**の空欄にする。
型カードを見ながら手が動く状態が、この教材のゴール。

### 6-5. 実装メモ(地下つき面積図)

- `.plane`(`position:relative`)の中に長方形を `position:absolute` で置き、
  `bottom` で地面からの高さを指定する。地面は `.ground`(1本の線)。
- **地面の線は長方形より前面に出す**(`.ground{z-index:3}` / `.plane .rect{z-index:1}`)。
  こうすると ○+☆ を地面の線が横切り、「地上3段 + 地下1段」が目で数えられる。
  これが 3+1 を暗記させないための肝。
- 地面のラベル(「はじめの位置」)は `right:100%` で左の余白に出す。
  図の中に置くと他の要素と重なる。
- ☆ の原理(6-2)を説明するステップでは、問題から離れた**横棒2本の抽象図**を使う。
  ○ と × に同じ幅の点線ブロックをつなげ、右はしのずれが変わらないことを見せる。
  棒の長さは実際の比で描くこと(差が本当に等しく見える)。

### 6-6. 答えが 0 になる空欄に注意

`ck()` は `Number(入力値)` で判定するため、**答えが 0 のステップでは空欄が正解扱いになる**
(`Number("") === 0`)。6-3 の手順1は「15−15=0」で必ずこれに当たる。
`benshozan-kyozai.html` のように空文字を先に弾くこと。

```js
var raw = String(document.getElementById('i'+step).value).trim();
if (raw !== '' && Number(raw) === ans) { /* 正解 */ }
```
